name: Test multiple search paths
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          search_paths: tests/fixtures/valid tests/fixtures/valid2

      - name: Assert action succeeded with multiple search paths
        run: |
          echo "PASS: action completed successfully scanning multiple search paths"
